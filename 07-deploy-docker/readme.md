# Deploy Docker Apps

Proyek ini menjelaskan proses installasi docker dan deployment container nginx kedalam VM yang sudah dibuat sebelumnya. Untuk cek jalan dan lihat verbose aplikasi: `ansible-playbook -v -i inventory.yml playbook.yml`
